server:
  port: 8084

spring:
  application:
    name: order_service






  datasource:
    url: jdbc:postgresql://localhost:5432/orderDB
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

